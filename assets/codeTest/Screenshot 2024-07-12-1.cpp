#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int solution(vector<int> A, vector<int> B) {
    int answer = -1;

    sort(begin(A), end(A));

    return answer;
}

int main()
{
    std::cout << "Hello World!\n";
}
